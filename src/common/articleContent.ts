import React, { FC } from "react";

interface ArticleProps {
    title:string;
    absract:string;
    body:any;
    image:string;
}
const Article=()=>{
    // return(
    // )
  
}
export default Article;